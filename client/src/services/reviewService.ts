import { apiService } from "./apiService";

// export const submitReview = async (review: any, token: string) => {
//     try {
//         await apiService.post("reviews", review, token);
//         alert("Review submitted successfully");
//     } catch (error) {
//         throw new Error("Failed to submit review");
//     }
// }
